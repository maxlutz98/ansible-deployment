---

- name: Extract backup to /opt/stacks/
  become: true
  become_user: root
  ansible.builtin.unarchive:
    src: backup/{{ inventory_hostname_short }}.tar.gz
    dest: /opt/stacks/
    copy: true
